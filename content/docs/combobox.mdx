---
title: Combobox
description: A searchable list of options with a button trigger and an accessible popup.
---

import { ComboboxPopupDemo, ComboboxPrimaryFlatDemo, ComboboxGhostDemo, ComboboxOutlineDemo, ComboboxGroupedDemo, ComboboxWithAvatarDemo } from "@/components/combobox-demo";
import { Tabs, Tab } from "fumadocs-ui/components/tabs";

A combobox lets users pick from a list inside an accessible popup. Use a button trigger with a search field in the popup when the list is long or needs filtering.

## Popup

Use a button as the trigger and place `ComboboxInput` inside `ComboboxContent` so users can search the list. Set `showTrigger={false}` on the input so the popup does not show a second chevron.

<Tabs items={['Preview', 'Code']} defaultIndex={0}>
  <Tab>
    <DocsPreview>
      <ComboboxPopupDemo />
    </DocsPreview>
   </Tab>
</Tabs>

## With Avatar

Display items with avatars and show the selected avatar in the trigger. Useful for user selectors and profile pickers.

<Tabs items={['Preview', 'Code']} defaultIndex={0}>
  <Tab>
    <DocsPreview>
      <ComboboxWithAvatarDemo />
    </DocsPreview>
  </Tab>
  <Tab>
    <div className="flex flex-col gap-4">
      <div className="bg-fd-muted/30 p-4 rounded-lg text-sm font-mono">

```tsx
"use client";

import {
  Combobox,
  ComboboxContent,
  ComboboxEmpty,
  ComboboxInput,
  ComboboxItem,
  ComboboxList,
  ComboboxTrigger,
  ComboboxValue,
} from "@/components/ui/combobox";
import { Avatar, AvatarImage, AvatarFallback } from "@/components/ui/avatar";

const users = [
  {
    id: "",
    value: "",
    name: "Select user",
    avatar: "",
  },
  {
    id: "1",
    value: "john-doe",
    name: "John Doe",
    avatar: "https://i.pravatar.cc/150?u=john",
  },
  {
    id: "2",
    value: "jane-smith",
    name: "Jane Smith",
    avatar: "https://i.pravatar.cc/150?u=jane",
  },
];

export function ComboboxWithAvatar() {
  return (
    <Combobox items={users}>
      <ComboboxTrigger className="w-64">
        <ComboboxValue placeholder="Select a user">
          {(item) => (
            <div className="flex items-center gap-2">
              <Avatar size="xs">
                <AvatarImage src={item.avatar} alt={item.name} />
                <AvatarFallback>
                  {item.name
                    .split(" ")
                    .map((n) => n[0])
                    .join("")}
                </AvatarFallback>
              </Avatar>
              <span>{item.name}</span>
            </div>
          )}
        </ComboboxValue>
      </ComboboxTrigger>
      <ComboboxContent>
        <ComboboxInput showTrigger={false} placeholder="Search users" />
        <ComboboxEmpty>No users found.</ComboboxEmpty>
        <ComboboxList>
          {(item) => (
            <ComboboxItem key={item.id} value={item}>
              <Avatar size="xs">
                <AvatarImage src={item.avatar} alt={item.name} />
                <AvatarFallback>
                  {item.name
                    .split(" ")
                    .map((n) => n[0])
                    .join("")}
                </AvatarFallback>
              </Avatar>
              <span>{item.name}</span>
            </ComboboxItem>
          )}
        </ComboboxList>
      </ComboboxContent>
    </Combobox>
  );
}
```

      </div>
    </div>

  </Tab>
</Tabs>


## Grouped Options

Organize related options with `ComboboxGroup` and `ComboboxLabel`. The popup search field filters items across all groups.

<Tabs items={['Preview', 'Code']} defaultIndex={0}>
  <Tab>
    <DocsPreview>
      <ComboboxGroupedDemo />
    </DocsPreview>
  </Tab>
  <Tab>
    <div className="flex flex-col gap-4">
      <div className="bg-fd-muted/30 p-4 rounded-lg text-sm font-mono">

```tsx
"use client";

import {
  Combobox,
  ComboboxTrigger,
  ComboboxValue,
  ComboboxContent,
  ComboboxInput,
  ComboboxList,
  ComboboxItem,
  ComboboxGroup,
  ComboboxLabel,
  ComboboxSeparator,
  ComboboxEmpty,
} from "@/components/ui/combobox";

export function GroupedComboboxDemo() {
  return (
    <Combobox defaultValue="next">
      <ComboboxTrigger className="w-64">
        <ComboboxValue placeholder="Select a product" />
      </ComboboxTrigger>
      <ComboboxContent>
        <ComboboxInput showTrigger={false} placeholder="Search" />
        <ComboboxEmpty>No results found.</ComboboxEmpty>
        <ComboboxList>
          <ComboboxGroup>
            <ComboboxLabel>Frameworks</ComboboxLabel>
            <ComboboxItem value="next">Next.js</ComboboxItem>
            <ComboboxItem value="nuxt">Nuxt</ComboboxItem>
            <ComboboxItem value="remix">Remix</ComboboxItem>
          </ComboboxGroup>
          <ComboboxSeparator />
          <ComboboxGroup>
            <ComboboxLabel>Platforms</ComboboxLabel>
            <ComboboxItem value="vercel">Vercel</ComboboxItem>
            <ComboboxItem value="netlify">Netlify</ComboboxItem>
            <ComboboxItem value="cloudflare">Cloudflare</ComboboxItem>
          </ComboboxGroup>
        </ComboboxList>
      </ComboboxContent>
    </Combobox>
  );
}
```

      </div>
    </div>

  </Tab>
</Tabs>

## Primary Flat

A subtle primary-toned trigger without a border. Use it when the combobox needs to blend into a primary-colored section.

<Tabs items={['Preview', 'Code']} defaultIndex={0}>
  <Tab>
    <DocsPreview>
      <ComboboxPrimaryFlatDemo />
    </DocsPreview>
  </Tab>
  <Tab>
    <div className="flex flex-col gap-4">
      <div className="bg-fd-muted/30 p-4 rounded-lg text-sm font-mono">

```tsx
"use client";

import {
  Combobox,
  ComboboxContent,
  ComboboxEmpty,
  ComboboxInput,
  ComboboxItem,
  ComboboxList,
  ComboboxTrigger,
  ComboboxValue,
} from "@/components/ui/combobox";
import { Icon } from "@/components/ui/icon";

const countries = [
  { code: "", value: "", label: "Select country" },
  { code: "ar", value: "argentina", label: "Argentina" },
  { code: "au", value: "australia", label: "Australia" },
  { code: "br", value: "brazil", label: "Brazil" },
  { code: "ca", value: "canada", label: "Canada" },
  { code: "fr", value: "france", label: "France" },
  { code: "de", value: "germany", label: "Germany" },
  { code: "jp", value: "japan", label: "Japan" },
  { code: "us", value: "united-states", label: "United States" },
];

export function ComboboxPrimaryFlat() {
  return (
    <Combobox items={countries} defaultValue={countries[0]}>
      <ComboboxTrigger variant="primary-light" className="w-64">
        <ComboboxValue />
      </ComboboxTrigger>
      <ComboboxContent>
        <ComboboxInput showTrigger={false} placeholder="Search" />
        <ComboboxEmpty>No items found.</ComboboxEmpty>
        <ComboboxList>
          {(item) => (
            <ComboboxItem key={item.code} value={item}>
              {item.label}
            </ComboboxItem>
          )}
        </ComboboxList>
      </ComboboxContent>
    </Combobox>
  );
}
```

      </div>
    </div>

  </Tab>
</Tabs>

## Ghost

A borderless and transparent trigger that reveals its content on hover. Ideal for inline or toolbar use cases where visual weight should be minimal.

<Tabs items={['Preview', 'Code']} defaultIndex={0}>
  <Tab>
    <DocsPreview>
      <ComboboxGhostDemo />
    </DocsPreview>
  </Tab>
  <Tab>
    <div className="flex flex-col gap-4">
      <div className="bg-fd-muted/30 p-4 rounded-lg text-sm font-mono">

```tsx
"use client";

import {
  Combobox,
  ComboboxContent,
  ComboboxEmpty,
  ComboboxInput,
  ComboboxItem,
  ComboboxList,
  ComboboxTrigger,
  ComboboxValue,
} from "@/components/ui/combobox";
import { Icon } from "@/components/ui/icon";

const countries = [
  { code: "", value: "", label: "Select country" },
  { code: "ar", value: "argentina", label: "Argentina" },
  { code: "au", value: "australia", label: "Australia" },
  { code: "br", value: "brazil", label: "Brazil" },
  { code: "ca", value: "canada", label: "Canada" },
  { code: "fr", value: "france", label: "France" },
  { code: "de", value: "germany", label: "Germany" },
  { code: "jp", value: "japan", label: "Japan" },
  { code: "us", value: "united-states", label: "United States" },
];

export function ComboboxGhost() {
  return (
    <Combobox items={countries} defaultValue={countries[0]}>
      <ComboboxTrigger variant="ghost" className="w-64">
        <ComboboxValue />
      </ComboboxTrigger>
      <ComboboxContent>
        <ComboboxInput showTrigger={false} placeholder="Search" />
        <ComboboxEmpty>No items found.</ComboboxEmpty>
        <ComboboxList>
          {(item) => (
            <ComboboxItem key={item.code} value={item}>
              {item.label}
            </ComboboxItem>
          )}
        </ComboboxList>
      </ComboboxContent>
    </Combobox>
  );
}
```

      </div>
    </div>

  </Tab>
</Tabs>
